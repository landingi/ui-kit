import { Item } from '../stories/landings/Item';
declare const data: {
    collection: Item[];
    pagination: {
        counter: {
            current: number;
            total: number;
        };
    };
};
export default data;
