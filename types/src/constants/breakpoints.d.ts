export namespace breakpoints {
    const biggerLaptop: number;
    const biggerMobile: number;
    const desktop: number;
    const laptop: number;
    const mobile: number;
    const tablet: number;
}
