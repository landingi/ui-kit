/**
 * inRange
 * Check if the number is in range
 * @param {Number} x
 * @param {Number} min
 * @param {Number} max
 * @return {Bool}
 */
export const inRange = (x, min, max) => Number(x) >= Number(min) && Number(x) <= Number(max)
