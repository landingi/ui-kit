import { Props, Playground } from 'docz'
import ProgressBar from '@components/ProgressBar'

# ProgressBar

<Props of={ProgressBar} />

## Variants

<Playground>
  <ProgressBar quantity={20} variant='success' />
  <br />
  <ProgressBar quantity={30} variant='warning' />
  <br />
  <ProgressBar quantity={40} variant='alert' />
  <br />
  <ProgressBar quantity={50} variant='progress' />
  <br />
  <ProgressBar quantity={60} variant='brand' />
</Playground>

## Size

<Playground>
  <ProgressBar quantity={60} size='small' variant='success' />
  <br />
  <ProgressBar quantity={60} variant='success' />
</Playground>

## Border

<Playground>
  <ProgressBar quantity={60} variant='progress' border='white' />
  <br />
  <ProgressBar quantity={60} variant='progress' border='grey' />
</Playground>

## Without animation

<Playground>
  <ProgressBar quantity={60} variant='brand' withoutAnimation />
</Playground>

## Show value

<Playground>
  <ProgressBar quantity={60} variant='progress' showValue valueSize={12} />
  <br />
  <ProgressBar quantity={60} variant='progress' showValue valueSize={16} />
  <br />
  <ProgressBar quantity={60} variant='progress' showValue valueSize={18} />
  <br />
  <ProgressBar quantity={60} variant='progress' showValue />
</Playground>

## Show limit alert

<Playground>
  <ProgressBar
    quantity={60}
    variant='progress'
    i18n={{ limitAlert: 'Limit not exceeded' }}
  />
  <br />
  <ProgressBar
    quantity={100}
    variant='warning'
    i18n={{ limitAlert: 'Limit reached' }}
  />
  <br />
  <ProgressBar
    quantity={150}
    variant='alert'
    i18n={{ limitAlert: 'Limit exceeded' }}
  />
</Playground>
