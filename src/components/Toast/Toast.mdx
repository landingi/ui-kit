import { Playground, Props } from 'docz'
import { emitToastToggle } from '@events/toast'
import Toast from '@components/Toast'
import { Button } from '@components/Button'

# Timing Toast

<Props of={Toast} />

## Basic usage

<Playground>
  {() => (
    <>
      <Button onClick={() => emitToastToggle('test', 'success')}>
        Open modal
      </Button>
      <Toast />
    </>
  )}
</Playground>
