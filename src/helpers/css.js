import classnames from 'classnames/bind'

/**
 * Exports styles from SCSS file
 * @param {object} styles
 * @return {object} An object of styles
 */
export const styles = styles => classnames.bind(styles)
